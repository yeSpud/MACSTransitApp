<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewmodel"
            type="fnsb.macstransit.activities.mapsactivity.MapsViewModel" />

        <variable
            name="activity"
            type="fnsb.macstransit.activities.mapsactivity.MapsActivity" />
    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/map"
            android:name="com.google.android.gms.maps.SupportMapFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:context=".activities.mapsactivity.MapsActivity" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="end"
            android:orientation="vertical"
            android:layout_margin="10dp"
            android:layout_gravity="top|end">

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/manage_routes"
                android:layout_height="wrap_content"
                android:layout_width="wrap_content"
                android:layout_gravity="top|end"
                android:backgroundTint="@color/colorPrimary"
                android:src="@drawable/amu_bubble_mask"
                app:fabSize="normal"
                android:onClick="@{() -> activity.showRoutesMenu()}"/>

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/view_fares"
                android:layout_height="wrap_content"
                android:layout_width="wrap_content"
                app:fabSize="mini"
                android:layout_marginTop="10dp"
                android:backgroundTint="@color/colorPrimary"
                android:src="@drawable/ic_launcher_foreground"
                android:onClick="@{() -> activity.farePopupWindow.showFarePopupWindow()}"/>

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/settings"
                android:layout_height="wrap_content"
                android:layout_width="wrap_content"
                app:fabSize="mini"
                android:backgroundTint="@color/colorPrimary"
                android:src="@drawable/settings"
                android:onClick="@{() -> activity.launchSettingsActivity()}"/>

        </LinearLayout>

    </FrameLayout>

</layout>